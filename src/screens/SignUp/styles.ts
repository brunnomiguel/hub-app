import styled from "styled-components/native";

export const PasswordViewController = styled.View`
  position: relative;
`;

export const BoxIcon = styled.TouchableOpacity`
  cursor: pointer;

  position: absolute;

  top: 14px;
  right: 6px;
`;
